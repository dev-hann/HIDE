
import 'package:flutter/material.dart';

class ActionMenuView extends StatelessWidget{
  const ActionMenuView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Card(
      child:Text("ActionMenuView")
    );
  }

}
